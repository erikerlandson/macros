### macros
Experiments with Scala macros

##### build

    # got to project directory
    $ cd /path/to/macros
    # or use 'xsbt compile'
    $ sbt compile

##### run

    $ scala -cp core/target/scala-2.11/classes Test

or

    # or use xsbt
    $ sbt run test
