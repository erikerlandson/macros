### macros
Experiments with Scala macros

### build

    # go to project directory
    $ cd /path/to/macros
    $ sbt compile
    # or use xsbt

### run
    $ sbt 'core/run-main Demo'
    # or use xsbt
